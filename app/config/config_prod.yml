imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: apc

#doctrine:
#    orm:
#        metadata_cache_driver:
#            type: service
#            id: cache.redis
#        result_cache_driver:
#            type: service
#            id: cache.redis
#        query_cache_driver:
#            type: service
#            id: cache.redis

#jms_serializer:
#    metadata:
#        cache: my.jms_serializer.cache

monolog:
    handlers:
        main:
            type:           fingers_crossed
            action_level:   error
            handler:        nested
        nested:
            type:   stream
            path:   "%kernel.logs_dir%/%kernel.environment%.log"
            level:  debug
        console:
            type:   console
