Tagcade\Entity\Core\NativeAdSlot:
    properties:
        name:
            - NotBlank: ~
            - Length:
                min: 2
                minMessage: "Ad slot name must be at least {{ limit }} characters in length"
        site:
            - NotBlank: ~
            - Valid: ~