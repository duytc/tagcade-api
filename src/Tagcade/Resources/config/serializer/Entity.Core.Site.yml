Tagcade\Entity\Core\Site:
    exclusion_policy: ALL
#    virtual_properties:
#        isRTBEnabled:
#            serialized_name: rtbEnabled
#            groups: [site.detail, site.summary]
    properties:
        id:
            expose: true
            groups: [site.detail, site.summary, site.minimum, site.primary]
        publisher:
            expose: true
            groups: [site.detail, site.summary]
        name:
            expose: true
            groups: [site.detail, site.summary, site.primary]
        domain:
            expose: true
            groups: [site.detail, site.minimum]
        enableSourceReport:
            expose: true
            groups: [site.detail, site.summary]
        autoCreate:
            groups: [site.detail, site.summary]
        players:
            expose: true
            groups: [site.detail, site.summary]
        rtbStatus:
            expose: true
            groups: [site.detail, site.summary]
