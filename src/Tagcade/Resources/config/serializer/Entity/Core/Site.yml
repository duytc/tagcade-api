Tagcade\Entity\Core\Site:
    exclusion_policy: ALL
    properties:
        id:
            expose: true
            groups: [site.detail, site.summary, site.minimum, site.primary]
        publisher:
            expose: true
            groups: [site.detail, site.summary]
        name:
            expose: true
            groups: [site.detail, site.summary, site.primary, site.minimum]
        domain:
            expose: true
            groups: [site.detail, site.minimum]
        enableSourceReport:
            expose: true
            groups: [site.detail, site.summary]
        autoCreate:
            groups: [site.detail, site.summary]
        players:
            expose: true
            groups: [site.detail, site.summary]
        subPublisher:
            expose: true
            groups: [site.detail, site.summary]